---

 - hosts: localhost
   remote_user: root

   roles:
     - ansible-role-dell-firmware-upgrade

   pre_tasks:
     - name: install wget
       package:
         name: wget
         state: present
